Air Quality Analysis
================

This analysis uses the built-in **airquality** dataset. A data frame is
created from it, then **cleaned using `complete.cases()`** to remove
rows with missing values.

The original dataset has **153 rows** with **44 NA values**. After
cleaning, **111 rows** remain with **0 NA values** — 42 incomplete rows
were removed.
